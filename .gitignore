__pycache__/
*.pyc
.venv/
venv/

# datasets (large, fetched separately)
data/raw/
data/raw_*/
data/*.zip
data/*.cache

# trained weights & training runs
weights/*.pt
weights/*.onnx
weights/*.cbm
weights/*.pth.tar
weights/*.tflite
weights/*.h5
runs/
*.pt
.pytest_cache/

# outputs
outputs/
*.mp4
*.avi
*.webm
!tests/fixtures/**

# one-off data transfer staging (dataset/weights copied in manually, not project source)
tennis_tracking_transfer/

.DS_Store

# graphify knowledge-graph build output (regenerate with /graphify)
graphify-out/

# Thumbnail strips for label candidates: ~340MB of JPEGs for eight clips,
# and fully regenerable from the clip plus its replay cache with
# scripts/extract_label_candidates.py. The label CSVs beside them ARE
# tracked - those hold the human judgements, which nothing can regenerate.
data/labels/skeletons/*_thumbs/
