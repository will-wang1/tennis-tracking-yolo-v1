__pycache__/
*.pyc
.venv/
venv/

# datasets (large, fetched separately)
data/raw/
data/raw_*/
data/*.zip
data/*.cache

# trained weights & training runs
weights/*.pt
weights/*.onnx
weights/*.cbm
weights/*.pth.tar
weights/*.tflite
weights/*.h5
runs/
*.pt
.pytest_cache/

# outputs
outputs/
*.mp4
*.avi
*.webm
!tests/fixtures/**

# one-off data transfer staging (dataset/weights copied in manually, not project source)
tennis_tracking_transfer/

.DS_Store

# graphify knowledge-graph build output (regenerate with /graphify)
graphify-out/

# web UI
.env
backend/.env
frontend/node_modules/
frontend/tsconfig.tsbuildinfo
# frontend/dist/ is intentionally committed (not ignored) - see frontend/Dockerfile's
# comment: it lets `docker compose build` work without npm/Node reachable at all,
# for environments where the npm registry is blocked. Rebuild it after any
# frontend source change: cd frontend && npm run build

