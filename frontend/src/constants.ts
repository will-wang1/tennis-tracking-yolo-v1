// sessionStorage key used to hand chosen job options from Upload -> Calibrate,
// since starting a calibration navigates away before the job is created.
export const PENDING_JOB_KEY = "tennis_tracking_pending_job";
export const DEFAULT_JOB_OPTIONS = { bounce: true, speed: true, sidebar: true, minimap: false };
